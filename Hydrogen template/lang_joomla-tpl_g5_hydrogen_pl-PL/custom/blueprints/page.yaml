name: Ustawienia strony
description: Ustawienia, które można zastosować do strony.

form:
  fields:

    doctype:
      type: input.text
      label: Deklaracji dokumentu html
      default: html

    body.class:
      type: input.text
      label: Klasa sekcji Body
      default: gantry
