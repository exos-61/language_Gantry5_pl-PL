name: Ustawienia strony
description: Ustawienia, które można zastosować na stronie.

form:
  fields:

    doctype:
      type: input.text
      label: Doctype
      default: html

    body.class:
      type: input.text
      label: Klasa Body
      default: gantry
