name: Blok
description: Layout block.
type: block

form:
  fields:
  
    class:
      type: input.selectize
      label: Klasy CSS
      description: Wprowadź nazwy klas CSS.

    extra:
      type: collection.keyvalue
      label: Atrybuty Tagu
      description: Dodatkowe atrybuty znaczników. Dodaj tylko wbudowany kod CSS, tagi < style >< /style > zostaną automatycznie dodane.
      key_placeholder: 'Key (data-*, style, ...)'
      value_placeholder: Wartość
      exclude: ['id', 'class']
